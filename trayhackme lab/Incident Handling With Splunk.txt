index=botsv1 imreallynotbatman.com sourcetype="suricata" src_ip="40.80.148.42"

One suricata alert highlighted the CVE value associated with the attack attempt. What is the CVE value?

cve-2014-6271

What is the CMS our web server is using?
 joomla
 
 4.3 What is the web scanner, the attacker used to perform the scanning attempts?
 
 
 Acunetix
 
 
What is the IP address of the server imreallynotbatman.com?

index=botsv1 imreallynotbatman.com src=40.80.148.42 sourcetype=suricata sourcetype=suricata "http.http_method"=ACUNETIX
 192.168.250.70
 
 
 5.1What was the URI which got multiple brute force attempts?
 
 index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" http_method=POST form_data=*username*passwd* | rex field=form_data "passwd=(?<creds>\w+)" |table _time src_ip uri http_user_agent creds
 
 - /joomla/administrator/index.php
 
 5.2 Against which username was the brute force attempt made?
 index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" http_method=POST uri="/joomla/administrator/index.php" form_data=*username*passwd* | table _time uri src_ip dest_ip form_data
 -admin
 
 5.3 What was the correct password for admin access to the content management system running imreallynotbatman.com?
 index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" http_method=POST form_data=*username*passwd* | rex field=form_data "passwd=(?<creds>\w+)" |table _time src_ip uri http_user_agent creds
 - batman
 How many unique passwords were attempted in the brute force attempt?
 index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" http_method=POST form_data=*username*passwd* | rex field=form_data "passwd=(?<creds>\w+)" |table _time src_ip uri http_user_agent creds
 -412
 
 5.4 What IP address is likely attempting a brute force password attack against imreallynotbatman.com?
 index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" http_method=POST form_data=*username*passwd* | rex field=form_data "passwd=(?<creds>\w+)" |table _time src_ip uri http_user_agent creds
 -23.22.63.114
 
 5.5 After finding the correct password, which IP did the attacker use to log in to the admin panel?
 index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" http_method=POST form_data=*username*passwd* | rex field=form_data "passwd=(?<creds>\w+)" |table _time src_ip uri http_user_agent creds
 - 40.80.148.42
 
6.1 Sysmon also collects the Hash value of the processes being created. What is the MD5 HASH of the program 3791.exe?
 index=botsv1 "3791.exe" sourcetype="XmlWinEventLog" EventCode=1 CommandLine="3791.exe"
 
 - AAE3F5A29935E6ABCC2C2754D12A9AF0
 
 
 6.2 Looking at the logs, which user executed the program 3791.exe on the server?
 index=botsv1 "3791.exe" sourcetype="XmlWinEventLog" EventCode=1 CommandLine="3791.exe"
 
 - Find user and under user you will see the name below
 - NT AUTHORITY\IUSR
 6.3 Search hash on the virustotal. What other name is associated with this file 3791.exe?
 index=botsv1 "3791.exe" sourcetype="XmlWinEventLog" EventCode=1 CommandLine="3791.exe"
 
 Visite http://www.virustotal.com/ put the hash no on search 
 direct after the hash no you will see the answer
 ab.exe